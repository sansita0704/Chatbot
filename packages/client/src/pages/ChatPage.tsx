import ChatBot from '@/components/chat/ChatBot';

const ChatPage = () => {
    return (
        <div className="h-screen p-5 w-2xl mx-auto">
            <ChatBot />
        </div>
    );
};

export default ChatPage;
