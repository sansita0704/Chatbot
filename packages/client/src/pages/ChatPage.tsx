import ChatBot from '@/components/chat/ChatBot';

const ChatPage = () => {
    return (
        <div className="h-screen p-5">
            <ChatBot />
        </div>
    );
};

export default ChatPage;
